"use strict";

describe("example test", () => {
    beforeEach(async () => {
        // Set up.
    });

    afterEach(async () => {
        // Cleanup.
    });

    test("should goes here", async () => {
        await expect(true);
    });
});
